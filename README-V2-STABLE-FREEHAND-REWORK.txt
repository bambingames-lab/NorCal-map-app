V2 stable freehand rework

Upload these files to the V2 branch root.

Fixes:
- Removes the app error caused by the prior freehand edit patch.
- Freehand coverage sits visually above ZIP fill colors.
- ZIP clicks work in ZIP Select Mode.
- Freehand only becomes clickable in Edit Drawings Mode.
- Tags are restored above freehand areas.
- Tags do not block ZIP clicks.

Drawing tools:
- Freehand draw
- Square shape
- Circle shape
- All shapes auto-fill and save to coverage_areas
- Shapes save with user tag, user color, team id, and team color

Workflow:
- Tap pencil > Freehand/Square/Circle to add coverage.
- Tap pencil > Edit Drawings Mode to edit/delete freehand.
- Tap pencil > ZIP Select Mode to return to normal ZIP clicking.
