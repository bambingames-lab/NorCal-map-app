# Territory Manager V2.1.1

## Fixed
- Freehand fill color is separate from ZIP/team fill color again.
- ZIP fill uses the team color.
- Freehand fill uses the user's drawing color.
- Freehand outline uses the team color.
- Phone freehand saves now retry with a simpler database row if optional V2 columns are missing.
- Freehand tags are smaller.

## Included
- Full V2 release package.
- Optional SQL file: `v2.1.1-coverage-save-schema-fix.sql`.
