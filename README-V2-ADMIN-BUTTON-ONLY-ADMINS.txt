V2 Admin button visibility patch

Upload these files to the V2 branch root.

Changes:
- Admin button is hidden by default.
- Admin button only appears after the signed-in user is confirmed in the admins table.
- Non-admin users cannot open the Admin Center even if they try to force it.
- Signed-out users do not see the Admin button.
