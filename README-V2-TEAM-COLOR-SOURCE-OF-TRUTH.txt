V2 team color source-of-truth patch

Upload these files to the V2 branch root.

Fixes:
- ZIP colors now render from the current team color.
- Freehand coverage colors now render from the current team color.
- Old saved area colors are only used as fallback.
- Changing a team color updates the map immediately.
- Added Admin > Teams > Repair Missing Coverage Teams.

Recommended:
1. Upload this patch.
2. Open V2.
3. Go to Admin > Teams.
4. Confirm PLV colors.
5. Tap Save Teams.
6. Tap Repair Missing Coverage Teams once if old freehand areas still look wrong.
